# ═══════════════════════════════════════════════════════
# Secrets — sensitive data not to be committed!
# In production use Vault / AWS Secrets Manager
# For demo purposes only
# ═══════════════════════════════════════════════════════
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
type: Opaque
stringData:
  postgres-password: "Monitor@2026!"
  postgres-connection: "Host=postgres-service;Database=financialmonitor;Username=monitor;Password=Monitor@2026!"
