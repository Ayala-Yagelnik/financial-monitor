# IMPORTANT: Never commit real values to git!
# Use: kubectl create secret generic financial-monitor-secrets \
#        --from-literal=postgres-connection-string="Host=postgres;..." \
#        --from-literal=redis-connection-string="redis:6379"
#
# The values below are base64-encoded placeholders only.
apiVersion: v1
kind: Secret
metadata:
  name: financial-monitor-secrets
type: Opaque
data:
  postgres-connection-string: REPLACE_WITH_BASE64_ENCODED_VALUE
  redis-connection-string: REPLACE_WITH_BASE64_ENCODED_VALUE
